import { ComingSoon } from "../../../../components/admin/ComingSoon";

export default function AdminPaymentsPage() {
  return <ComingSoon title="Payments" note="Razorpay payment tracking arrives once payment credentials are configured (see .env.example)." />;
}
